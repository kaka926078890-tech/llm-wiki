# Keep cloned repos out of llm-wiki git. Do NOT use a blanket `*` here —
# CodeGraph reads .gitignore when scanning and would index zero files under code/.
/chatkit-middleware/
/chatkit-web/
/finclaw/
/.codegraph/
!.gitignore
!.gitkeep
