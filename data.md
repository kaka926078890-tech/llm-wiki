{
    "jsonrpc": "2.0",
    "id": "web-mcp-ask",
    "result": {
        "content": [
            {
                "type": "text",
                "text": "以下是 **chatkit-web** monorepo 中所有配置的综合分类。\n\n---\n\n## 1. 工作区根配置\n\n**package.json** — 包含 6 个工作区的 monorepo：`chatkit-admin-mt`、`chatkit-mobile`、`finclaw-frontend`、`libs/contracts-ts`、`libs/chatkit-native-chat`、`libs/rbac-ui`。\n\n---\n\n## 2. 构建工具配置（Vite + TypeScript）\n\n### 每个子项目的 Vite 配置\n\n| 子项目 | 端口 | base path | 备注 |\n|--------|------|-----------|------|\n| **chatkit-admin-mt** | 5175 | `VITE_BASE_PATH` 默认为 `/` | Tailwind CSS v4 插件；RBAC UI 别名；Cookie-first auth 代理 |\n| **chatkit-mobile** | 5173 或 `VITE_DEV_SERVER_PORT` | `VITE_BASE_PATH` 默认为 `/` | 优化排除 `@finogeeks2026/chatkit-web`；AG-UI、identity、MCP、API 代理 |\n| **finclaw-frontend** | 5185（严格模式） | `VITE_BASE_PATH` 默认为 `/` | 自定义代理插件；完整的代理表 |\n\n### 测试配置\n\n仅 `finclaw-frontend` 有专用的测试配置：测试环境为 `node`，路径模式为 `src/**/*.test.ts`。\n\n### TypeScript 配置\n\n| 文件 | target | 严格模式 | 备注 |\n|------|--------|----------|------|\n| `chatkit-admin-mt` | ES2020 | 是 | 排除测试文件 |\n| `chatkit-mobile` | ES2020 | 是 | 包含 `vite.config.ts` |\n| `finclaw-frontend` | ES2020 | 是 | 路径别名：`@/*`、`@chatkit-middleware/contracts-ts/*`、`@chatkit-web/native-chat` |\n| `libs/chatkit-native-chat` | ES2022 | 是 | 排除测试文件 |\n| `libs/rbac-ui` | ES2022 | 是 | |\n\n---\n\n## 3. 运行时环境变量\n\n### chatkit-admin-mt\n\n| 变量 | 默认值 | 用途 |\n|------|--------|------|\n| `VITE_AGENT_URL` | `window.origin + \"/agent\"` | AG-UI agent URL |\n\n### chatkit-mobile\n\n| 变量 | 默认值 | 用途 |\n|------|--------|------|\n| `VITE_BASE_PATH` | `/` | URL base path |\n| `VITE_DOCUMENTS_BASE_URL` | — | 文档基础 URL |\n| `VITE_API_GATEWAY_URL` | — | API 网关 |\n| `VITE_DEV_APP_ORIGIN` | — | 开发应用 origin |\n| `VITE_DEV_SERVER_PORT` | 5173 | 开发服务器端口 |\n| `VITE_PROXY_AGENT_TARGET` | `http://localhost:26100` | AG-UI 代理目标 |\n| `VITE_PROXY_IDENTITY_TARGET` | `http://localhost:26101` | Identity 服务代理 |\n| `VITE_PROXY_API_TARGET` | AG-UI target | API 代理 |\n| `VITE_PROXY_MCP_TARGET` | `http://localhost:8787` | MCP 代理 |\n| `VITE_IDENTITY_URL` | window origin | Identity 基础 URL |\n| `VITE_PERSONA_URL` | window origin | Persona 基础 URL |\n| `VITE_CLAW_AGENT_ID` | `\"default\"` | 默认 claw agent ID |\n| `VITE_CHANNEL_LOGIN_EMAIL` | — | 频道登录邮箱 |\n| `VITE_CHANNEL_LOGIN_PASSWORD` | — | 频道登录密码 |\n\n### finclaw-frontend\n\n| 变量 | 默认值 | 用途 |\n|------|--------|------|\n| `VITE_AGENT_URL` | `window.origin + \"/agent\"` | AG-UI agent |\n| `VITE_IDENTITY_URL` | window origin | Identity 基础 |\n| `VITE_PERSONA_URL` | window origin | Persona |\n| `VITE_DOC_TOOLKIT_URL` | `window.origin + \"/doc-toolkit\"` | Doc-toolkit |\n| `VITE_DOCUMENT_SERVICE_URL` | `window.origin + \"/api\"` | Document service |\n| `VITE_AI_INFRA_CRON_URL` | `window.origin + \"/ai-infra-rs/internal/cron\"` | AI infra cron |\n| `VITE_BASE_PATH` | `/` | Base URL |\n| `VITE_PROXY_AGENT_TARGET` | `http://localhost:26100` | AG-UI 上游 |\n| `VITE_PROXY_IDENTITY_TARGET` | `http://localhost:26101` | Identity 上游 |\n| `VITE_PROXY_PERSONA_TARGET` | `http://localhost:26100` | Persona 上游 |\n| `VITE_PROXY_SKILL_TARGET` | `http://localhost:26100` | Skills 上游 |\n| `VITE_PROXY_ONBOARDING_BACKEND` | `\"persona\"` | 接线后端选择 |\n| `VITE_PROXY_SKILLS_BACKEND` | `\"skill\"` | Skills 后端选择 |\n| `VITE_PROXY_AI_INFRA_RS_TARGET` | gateway (26100) | AI infra RS 上游 |\n| `VITE_PROXY_AI_INFRA_RS_DIRECT` | `\"0\"` | 跳过网关直接到 AI infra |\n| `VITE_PROXY_DOC_TOOLKIT_TARGET` | `http://localhost:26461` | Doc-toolkit 上游 |\n| `VITE_PROXY_DOCUMENT_SERVICE_TARGET` | `http://localhost:26205` | Document service |\n| `VITE_PROXY_TRIGGER_SCHEDULER_TARGET` | `http://localhost:26302` | Trigger scheduler |\n| `VITE_PROXY_FEISHU_TARGET` | `http://localhost:26460` | Feishu connector |\n| `VITE_PROXY_DINGTALK_TARGET` | `http://localhost:26462` | DingTalk connector |\n| `VITE_PROXY_WECOM_TARGET` | `http://localhost:26550` | WeCom connector |\n| `VITE_PROXY_WECOM_BIND_TARGET` | gateway (26100) | WeCom 绑定 |\n\n---\n\n## 4. Vite 开发代理配置\n\n所有三个应用在开发时都配置了 `changeOrigin: false` 的 HTTP 代理路由（以确保 cookie-based CSRF 检查通过）。详情如下：\n\n- **chatkit-admin-mt** — 代理 `/auth`、`/admin-api`、`/admin/users`、`/admin/tenants`、`/admin/rbac`、`/admin/skills`、`/admin/templates`、`/skills`、`/persona-uploads`、`/ai-infra` 等到各自的后端\n- **chatkit-mobile** — 代理 `/agent`、`/info`、`/health`、`/events`、`/auth`、`/users/me/agents`、`/users/me/prompt-tree`、`/api`、`/mcp`\n- **finclaw-frontend** — 最完整的代理表，覆盖了上述所有路径加上 `/me`、`/doc-toolkit`、`/document-svc`、`/trigger-scheduler`、`/delivery`、`/inbox`、`/api/feishu`、`/api/dingtalk`、`/api/wecom`\n\n---\n\n## 5. shadcn/ui 配置\n\n`finclaw-frontend` 使用 shadcn/ui 框架，配置为：样式 `new-york`，非 RSC，使用 TSX，Tailwind CSS 配置（CSS 文件 `src/index.css`，基础色 `neutral`，启用 CSS 变量），组件别名 `@/components`，工具函数别名 `@/lib/utils`，UI 组件别名 `@/components/ui`，图标库 `lucide`。\n\n---\n\n## 6. 本地化 / i18n 配置\n\n| 子项目 | 语言 | 检测策略 |\n|--------|------|----------|\n| **chatkit-admin-mt** | en, zh | `localStorage` → `navigator`；缓存键 `chatkit-admin-lang` |\n| **finclaw-frontend** | en, zh | `localStorage` 键 `finclaw-frontend-locale`（含从旧键的迁移） |\n\n---\n\n## 7. 主题配置\n\n`finclaw-frontend` 为纯亮色模式。`applyTheme()` 移除所有 `dark` 类并设置 `data-theme=\"light\"`。\n\n---\n\n## 8. 权限/RBAC 配置\n\n| 文件 | 角色 |\n|------|------|\n| `finclaw-frontend` | 定义前端权限门：`canBrowseSkills`、`canInstallSkill`、`canPublishSkill`、`canDeleteSkill`、`canUseTemplates`、`canAccessAgents` 等，基于 `@chatkit-web/rbac-ui` 中的权限检查 |\n| `chatkit-admin-mt` | 从 `@chatkit-web/rbac-ui` 重新导出 RBAC 工具 |\n\n---\n\n## 9. 频道配置\n\n两个应用中已定义可用 IM 集成：\n\n| 频道 | 状态 |\n|--------|--------|\n| Feishu | available |\n| DingTalk | available |\n| WeCom | available |\n\n---\n\n## 10. 会话/缓存配置\n\n`finclaw-frontend` 使用 localStorage session 缓存，配置为：最大缓存会话数 20，缓存 TTL 24 小时，localStorage 键前缀 `chatkit-sessions-v1-`、`chatkit-messages-v1-`、`chatkit-messages-index-v1-`。\n\n---\n\n## 11. 代码约定配置\n\n`.gitignore` 禁止提交 `.env`、`.env.*`、`.npmrc` 及其他构建产物。无独立的 `.env.example` 存在于 `chatkit-web` 中。\n\n以上是 chatkit-web 所有配置的完整分类。"
            }
        ],
        "isError": false
    }
}